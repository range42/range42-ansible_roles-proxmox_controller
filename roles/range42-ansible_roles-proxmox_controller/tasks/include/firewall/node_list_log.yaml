---
##
## READ THE FIREWALL LOG OF ONE NODE.
##
## GET /nodes/{node}/firewall/log - optional node_fw_log_limit / _start / _since / _until
## (since/until are unix epochs), passed through as-is.
##
## >>> AN EMPTY LOG IS NOT AN EMPTY LIST <<< Measured : an empty journal returns ONE row
## {n: 1, t: "no content"}. Published here as zero lines plus one object carrying
## node_fw_log_empty - never as one entry.
##
## >>> AN EMPTY LOG DOES NOT MEAN NOTHING WAS REFUSED <<< Measured : logging is off by
## default (no log_level_* in the options), real denies leave no line. Pair this read with
## firewall_node_list_options, which reports the log levels.
##
## This fact APPENDS across calls - reset it before reading, like the other collectors.
##

- block:
    ####
    ####

    - name: FIREWALL - NODE - READ LOG - BUILD THE QUERY
      set_fact:
        _fw_log_qs: >-
          {%- set p = [] -%}
          {%- if node_fw_log_limit is defined -%}{%- set _ = p.append('limit=' ~ node_fw_log_limit) -%}{%- endif -%}
          {%- if node_fw_log_start is defined -%}{%- set _ = p.append('start=' ~ node_fw_log_start) -%}{%- endif -%}
          {%- if node_fw_log_since is defined -%}{%- set _ = p.append('since=' ~ node_fw_log_since) -%}{%- endif -%}
          {%- if node_fw_log_until is defined -%}{%- set _ = p.append('until=' ~ node_fw_log_until) -%}{%- endif -%}
          {{ ('?' ~ (p | join('&'))) if p else '' }}

    - name: FIREWALL - NODE - READ LOG
      uri:
        url: "https://{{ proxmox_api_host }}/api2/json/nodes/{{ proxmox_node }}/firewall/log{{ _fw_log_qs }}"
        method: GET
        headers:
          Authorization: "PVEAPIToken={{ proxmox_api_user }}!{{ proxmox_api_token_id }}={{ proxmox_api_token_secret }}"
        validate_certs: "{{ proxmox_api_validate_certs | default(false) }}"
      register: proxmox_call

    - name: FIREWALL - NODE - READ LOG - PRINT proxmox_call
      debug:
        var: proxmox_call

    ####
    ## The single {n: 1, t: "no content"} row is the api's way of saying empty - a real line
    ## always carries the "<vmid> <level> - <timestamp> <message>" prefix.
    ####

    - name: FIREWALL - NODE - READ LOG - NORMALIZE THE EMPTY JOURNAL
      set_fact:
        _fw_log_lines: >-
          {{ [] if ((proxmox_call.json.data | length) == 1 and
                    (proxmox_call.json.data[0].t | default('')) == 'no content')
             else proxmox_call.json.data }}

    - name: FIREWALL - NODE - READ LOG - BUILD JSON
      set_fact:
        firewall_node_list_log: >-
          {{ (firewall_node_list_log | default([])) +
            [ {
              'action': "firewall_node_list_log",
              'source': "proxmox",
              'proxmox_node':   proxmox_node ,

              'node_fw_log_n':  ( item.n | default(omit)),
              'node_fw_log_t':  ( item.t | default(omit)),
            } ] }}
      loop: "{{ _fw_log_lines }}"
      loop_control:
        loop_var: item

    - name: FIREWALL - NODE - READ LOG - AN EMPTY JOURNAL IS A READING
      set_fact:
        firewall_node_list_log: >-
          {{ (firewall_node_list_log | default([])) +
            [ {
              'action': "firewall_node_list_log",
              'source': "proxmox",
              'proxmox_node':   proxmox_node ,
              'node_fw_log_empty': true,
            } ] }}
      when: (_fw_log_lines | length) == 0

    - name: FIREWALL - NODE - READ LOG - PRINT JSON
      debug:
        var: firewall_node_list_log
      when:
        - firewall_node_list_log is defined
        - firewall_node_list_log | length > 0

  ####
  ####

  when: proxmox_vm_action == "firewall_node_list_log"
